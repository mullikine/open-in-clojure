(ns open-in-clojure.core
  (:gen-class))

(defn fp fptype
  "Opens the file given by the path into a clojure object."
  []
  )


(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
